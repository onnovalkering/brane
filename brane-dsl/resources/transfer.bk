import "alloc" // loads 'allocate_temp_directory'
import "cp" // loads 'copy-to'

// The input is a remote resource
resource := ?? as Surl

// Create a new temporary directory
directory := allocate_temp_directory

// Copy the resource to the directory
file := copy resource to directory

return file // Stop, and mark 'file' as output