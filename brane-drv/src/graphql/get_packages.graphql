query GetPackages {
    packages {
        created,
        description,
        detached,
        functionsAsJson,
        id,
        kind,
        name,
        owners,
        typesAsJson,
        version
    }
}